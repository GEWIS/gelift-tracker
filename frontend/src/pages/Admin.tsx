export function AdminPage() {
    return (
        <div className="min-h-screen flex flex-col items-center justify-center gap-4 bg-slate-50 p-6">
            <h1 className="text-2xl font-semibold text-slate-800 m-0">Admin</h1>
            <p className="text-slate-600 m-0 max-w-md text-center">
                This area is authenticated via cookie. Add your admin tools here later.
            </p>
        </div>
    )
}
